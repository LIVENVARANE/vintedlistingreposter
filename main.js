function repost() {
    var isDone = false;

    var msg = document.getElementById('msg');
    var token = document.getElementById('token');
    var listing = document.getElementById('listing');

    //started doing this and then realized i dont have any proxy debugger on this pc... aborting mission bruh

    if(!isDone) {
        msg.innerText = 'An error happened, please retry.';
        msg.style.color = 'red';
    }
}